# 6nz — flogence intake queue (F4)

Auto-populated by the vPA deputy: bounded auto-intake of `from: flogence` messages (see `vpa.md` § F4).
The deputy **FILES** here; it does **NOT** triage. Each filed row captures: date · subject · reproducer
path · the reporter's expected-vs-actual (verbatim). The PA promotes a queued bug into `master-list.md §F`
WITH triage, then strikes the line here.

**Boundary recap** (user-ratified S15, "bounded auto-act, unattended"):
- AUTO-INTAKE → filed here + message archived `incoming/ → read/`: status · FYI · version/doc notice ·
  bug report (with reproducer).
- SURFACE → left in `incoming/`, routed to PA, NOT filed here: design-decision replies · 6nz↔flogence
  scope/architecture changes · anything touching the live build · non-flogence senders · anything ambiguous.

## Pending (PA triage)
*(none — queue empty at S15 adoption)*

## Promoted (→ §F, struck)
*(none)*
